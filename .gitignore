# macOS
.DS_Store
# Xcode build artifacts
build/
DerivedData/
*.xcarchive
*.ipa
*.dSYM.zip
*.dSYM
# Xcode user state (machine-specific UI layout, breakpoints)
*.xcuserstate
xcuserdata/
# Swift Package Manager
.build/
.swiftpm/
# CocoaPods (not used, but just in case)
Pods/
# Python cache
__pycache__/
*.pyc
# Generated analysis output (keep the folder's README)
results/*
!results/README.md
venv/
# Isolated ML venv (created by scripts/setup_ml_env.sh)
.venv-ml/
# Claude / project notes
# CLAUDE.md and .claude/ are shared (project context for collaborators + agents).
# Keep machine-specific bits out:
.claude/settings.local.json
build_log.md
logs/

#Test Data Images
Model-Training-Test/hand_images
Model-Training-Test/hand_manifest*.csv
Model-Training-Test/*.log
# Per-session IMU CSVs merged from app exports
Model-Training-Test/imu/
# Raw per-participant export folders (kept locally for provenance)
Model-Training-Test/exports/

# Agent/codex scratch artifacts (node_modules, rendered slides, etc.)
.tmp/

# Local agent design docs (plans/specs) — keep out of history
docs/superpowers/

# Analysis output frames, sample/threshold test exports, and scratch work
# (kept locally for reference, out of git)
tran_key_frames/
csv_threshold_test/
scratchpad/
